version: 2

jobs:
  build:
    docker:
      - image: cimg/base:2022.09
    steps:
      - checkout
      - apt-get install g++ clang git make libgtk-3-dev libnotify-dev libbz2-dev libssl-dev xdotool
      - run: make -f Makefile
      - run: make -f umkMakefile.in
