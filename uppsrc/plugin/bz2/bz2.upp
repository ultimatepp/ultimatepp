description "BZ2 compressor/decompressor\3770,128,128";

options(BUILDER_OPTION) NOWARNINGS;

uses
	Core;

library(POSIX & !NOSO) bz2;

library(WIN32) bz2;

external_dependency(VCPKG) bzip2;

external_dependency(DPKG) libbz2-dev;

file
	bz2.h,
	bz2upp.cpp,
	src.tpp,
	Copying;

