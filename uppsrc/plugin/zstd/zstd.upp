description "zstd compressor (good ratio, very good speed)\3770,128,128";

uses
	Core;

library
	zstd;

external_dependency(VCPKG) zstd;

external_dependency(DPKG) libzstd-dev;

file
	zstd.h,
	Compress.cpp,
	Util.cpp,
	Decompress.cpp,
	Copying,
	Info readonly separator;

