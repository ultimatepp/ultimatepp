description "Wordprocessor application\377";

uses
	CtrlLib,
	RichEdit,
	PdfDraw,
	plugin\jpg;

uses(WINFB | SDLFB) Framebuffer;

uses(SKELETON) Skeleton;

uses(WINFB) WinFb;

uses(WINALT) WinAlt;

uses(SDLFB) SDLFb;

file
	UWord.cpp,
	UWord.iml;

mainconfig
	"" = "GUI WINFB",
	"" = "GUI WINALT",
	"" = "GUI SKELETON",
	"" = "GUI SDLFB";

