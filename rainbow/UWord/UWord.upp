description "Wordprocessor application\377";

uses
	CtrlLib,
	RichEdit,
	PdfDraw,
	plugin\jpg;

uses(SKELETON) Skeleton;

uses(WINALT) WinAlt;

uses(WINFB) WinFb;

uses(LINUXFB) LinuxFb;

uses(SDLFB) SDLFb;

uses(WINGL) WinGl;

file
	UWord.cpp,
	UWord.iml;

mainconfig
	"" = "GUI SKELETON",
	"" = "GUI WINALT",
	"" = "GUI WINFB",
	"" = "GUI LINUXFB",
	"" = "GUI SDLFB",
	"" = "GUI WINGL",
	"" = "GUI";

