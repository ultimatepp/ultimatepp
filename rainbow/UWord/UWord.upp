description "Wordprocessor application\377";

uses
	CtrlLib,
	RichEdit,
	PdfDraw,
	plugin\jpg;

uses(SKELETON) Skeleton;

uses(WINFB) WinFb;

uses(WINALT) WinAlt;

uses(SDLFB) SDLFb;

uses(WINGL) WinGl;

file
	UWord.cpp,
	UWord.iml;

mainconfig
	"" = "GUI WINFB",
	"" = "GUI WINALT",
	"" = "GUI SKELETON",
	"" = "GUI SDLFB",
	"" = "GUI WINGL",
	"" = "GUI";

